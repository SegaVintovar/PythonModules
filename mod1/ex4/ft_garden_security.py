class SecurePlant:
	# name = None
	# height = None
	# age = None
	def __init__(self, name, height, age):
		self.name = name
		self.height = height
		self.age = age
	def set_height(self, size):
		if size >=0:
			self.height = size
		else
			print('')
	def set_age(self, days):
		if days >= 0:
			self.age = days
